import 'package:flutter/material.dart';

class Compound3Page extends StatelessWidget {
  const Compound3Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
