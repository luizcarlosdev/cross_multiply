import 'package:flutter/material.dart';

class Compound4Page extends StatelessWidget {
  const Compound4Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
