import 'package:flutter/material.dart';

class CompoundFourPage extends StatelessWidget {
  const CompoundFourPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Composta de 4"),
      ),
    );
  }
}
