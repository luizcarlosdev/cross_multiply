import 'package:flutter/material.dart';

class CompoundThreePage extends StatelessWidget {
  const CompoundThreePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Composta de 3"),
      ),
    );
  }
}
